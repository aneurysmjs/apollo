// @flow strict
import React from 'react';
import { Routing } from '@/core/Routing';
import { Layout } from '@/core/Layout';

const App = () => (
  <Layout>
    <Routing />
  </Layout>
);

export default App;
